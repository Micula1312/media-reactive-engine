const layers={a:{host:document.querySelector('#layer-a'),video:document.querySelector('#layer-a video'),image:document.querySelector('#layer-a img'),last:null,lastCutToken:null},b:{host:document.querySelector('#layer-b'),video:document.querySelector('#layer-b video'),image:document.querySelector('#layer-b img'),last:null,lastCutToken:null}};
const blackout=document.querySelector('#blackout-layer'),beatFlash=document.querySelector('#beat-flash'),textOverlay=document.querySelector('#text-overlay');
async function json(url){const r=await fetch(url,{cache:'no-store'});return r.json()}
function loadLayer(layer,media){if(!media){layer.video.style.display='none';layer.image.style.display='none';layer.last=null;return}if(layer.last===media.path)return;if(media.kind==='video'){layer.image.style.display='none';layer.video.style.display='block';layer.video.src=media.url;layer.video.load();layer.video.play().catch(()=>{})}else{layer.video.style.display='none';layer.image.style.display='block';layer.image.src=media.url}layer.last=media.path;layer.lastCutToken=null}
function applyRandomCut(layer,deck){if(deck.media?.kind!=='video'||!deck.cut_token||deck.cut_token===layer.lastCutToken||!Number.isFinite(layer.video.duration)||layer.video.duration<=0)return;layer.video.currentTime=Math.max(0,Math.min(.98,Number(deck.cut_position??Math.random())))*layer.video.duration;layer.lastCutToken=deck.cut_token}
function clamp(v,min,max){return Math.max(min,Math.min(max,v))}
function audioEnergy(a){return clamp((a.level||0)*.28+(a.bass||0)*.42+(a.mid||0)*.18+(a.high||0)*.12+(a.beat?.18:0),0,1)}
function arValue(v,a,baseKey,reactiveKey,amountKey,neutral,span=1){if(!v[reactiveKey])return Number(v[baseKey]??neutral);const amount=clamp(Number(v[amountKey]||0),0,1);return neutral+amount*audioEnergy(a)*span}
function buildMasterFilter(v,a){const intensity=+v.common_intensity||0,shared=+v.common_filter||0;let brightness=arValue(v,a,'master_brightness','master_brightness_reactive','master_brightness_amount',1,.9)*(1+intensity*.18);let contrast=arValue(v,a,'master_contrast','master_contrast_reactive','master_contrast_amount',1,1.2)*(1+intensity*.35);let blur=arValue(v,a,'master_blur','master_blur_reactive','master_blur_amount',0,12)+Math.max(0,-shared)*2.5;const sat=(+v.master_saturation||1)*(shared<0?1-Math.abs(shared)*.8:1+shared*1.2),hue=(+v.master_hue||0)+Math.max(0,shared)*80;const invert=clamp(arValue(v,a,'fx_invert','fx_invert_reactive','fx_invert_amount',0,1),0,1);return `brightness(${Math.max(0,brightness)}) contrast(${Math.max(0,contrast)}) saturate(${Math.max(0,sat)}) hue-rotate(${hue}deg) blur(${Math.max(0,blur)}px) invert(${invert})`}
function applyDeck(name,deck,opacity,a,v){const layer=layers[name];loadLayer(layer,deck.media);applyRandomCut(layer,deck);const target=deck.media?.kind==='video'?layer.video:layer.image;if(!target)return;const beat=a.beat?1:0,pulse=+v.common_pulse||0,energy=audioEnergy(a);
let scale;if(deck.scale_reactive){const amount=clamp(+deck.scale||0,0,1);scale=1+amount*energy*.58+beat*amount*.08}else scale=+deck.scale||1;scale*=1+beat*pulse*.09;
let deckOpacity;if(deck.opacity_reactive){const amount=clamp(+deck.opacity||0,0,1);deckOpacity=clamp(1-energy*amount*.58-beat*amount*.15,.08,1)}else deckOpacity=clamp(+deck.opacity||0,0,1);
let playbackRate;if(deck.speed_reactive){const amount=clamp(+deck.speed||0,0,1);playbackRate=clamp(1+energy*amount*1.35+beat*amount*.28,.2,3)}else playbackRate=clamp(+deck.speed||1,.2,3);
const glitch=clamp(arValue(v,a,'fx_glitch','fx_glitch_reactive','fx_glitch_amount',0,1),0,1),rgb=clamp(arValue(v,a,'fx_rgb','fx_rgb_reactive','fx_rgb_amount',0,1),0,1);if(glitch>0){const gx=(Math.random()-.5)*glitch*24,gy=(Math.random()-.5)*glitch*8;target.style.transform=`translate(${gx}px,${gy}px) scale(${scale}) skewX(${(Math.random()-.5)*glitch*3}deg)`}else target.style.transform=`scale(${scale})`;target.style.filter=buildMasterFilter(v,a);target.style.boxShadow=rgb>0?`${-rgb*10}px 0 0 rgba(255,0,0,${rgb*.45}), ${rgb*10}px 0 0 rgba(0,255,255,${rgb*.45})`:'none';layer.host.style.opacity=opacity*deckOpacity*(+v.master_opacity??1)*(+v.global_master??1);if(deck.media?.kind==='video'){layer.video.playbackRate=playbackRate;if(deck.playing&&layer.video.paused)layer.video.play().catch(()=>{});if(!deck.playing&&!layer.video.paused)layer.video.pause()}}
function applyText(v,a){if(!textOverlay)return;textOverlay.textContent=v.text_content||'';textOverlay.style.display=v.text_enabled&&v.text_content?'block':'none';textOverlay.style.fontSize=`${Math.max(12,+v.text_size||64)}px`;textOverlay.style.opacity=Math.max(0,Math.min(1,+v.text_opacity||0))*(+v.global_master??1);const pulse=a.beat?(+v.common_pulse||0)*.08:0;textOverlay.style.transform=`translate(-50%,-50%) scale(${1+pulse})`}
async function tick(){try{const[v,a]=await Promise.all([json('/api/visual-state'),json('/api/audio-state')]);const x=Math.max(0,Math.min(1,v.crossfader));applyDeck('a',v.decks.a,Math.cos(x*Math.PI/2),a,v);applyDeck('b',v.decks.b,Math.sin(x*Math.PI/2),a,v);applyText(v,a);blackout.classList.toggle('on',v.blackout);const strobe=a.beat?(+v.common_strobe||0)*.85:0;beatFlash.style.opacity=strobe}catch(e){console.error(e)}}setInterval(tick,45);tick();
